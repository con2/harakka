# Quick setup

```shell
infisical login         # Authenticate with Infisical Cloud or your self-hosted instance
infisical init          # Creates a .infisical.json config file
infisical run --env=dev --path=/ [your start command]  # Inject secrets when running your app
```