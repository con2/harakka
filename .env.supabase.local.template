# Supabase Local Development Environment Variables (example)
# Copy this file to `.env.supabase.local` and adjust values as needed.
ENV=.env.supabase.local
# ========================================
# ENVIRONMENT
# ========================================
NODE_ENV=development

# ========================================
# SUPABASE (Local CLI)
# ========================================
# Default local API URL and keys. These are safe for local use only.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=your-local-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-local-service-role-key
SUPABASE_JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters-long

# ========================================
# BACKEND
# ========================================
PORT=3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5180,http://localhost:5173

# ========================================
# FRONTEND (Vite)
# ========================================
VITE_API_URL=http://127.0.0.1:3000
VITE_SUPABASE_URL=${SUPABASE_URL}
VITE_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

# ========================================
# EMAIL (optional)
# ========================================
# Reuse values from your `.env.local` if needed.
STORAGE_EMAIL=YOUR_GMAIL_EMAIL
STORAGE_EMAIL_PASSWORD=YOUR_GMAIL_APP
# ========================================
# AUTH PROVIDERS (Google OAuth)
# ========================================
# Set ENABLED=true only if you have a Google OAuth Client for local dev
SUPABASE_AUTH_EXTERNAL_GOOGLE_ENABLED=false
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
# When enabling Google, ensure this redirect URI exists in Google Cloud Console:
#   ${SUPABASE_URL}/auth/v1/callback


# ========================================
# OPTIONAL
# ========================================
# RECAPTCHA_SECRET=
# SUPABASE_STORAGE_URL=
# S3_REGION=
# S3_BUCKET=

# ========================================
# CRON / SCHEDULER CONFIGURATION
# ========================================
# Use this shared secret to protect /cron/* endpoints (set the same in your scheduler)
CRON_SECRET="your-cron-shared-secret"